# frontend/

Next.js 15 (App Router) + Tailwind + shadcn/ui for TruthKeeper. Deploys to Vercel.

## Run locally

```bash
cp .env.local.example .env.local
# Edit .env.local with your local backend URL
pnpm dev
```

## Deploy

```bash
vercel --prod
```
